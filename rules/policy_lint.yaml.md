# Policy Lint Specification

```yaml
checks:
  unsupported_claims: fail
  stale_high_risk_claims: fail
  ignored_source_conflict: fail
  generic_language: warn
  redundant_context: warn
  low_vitality_prose: warn
  tone_drift: warn
  workflow_overhead: warn
```
